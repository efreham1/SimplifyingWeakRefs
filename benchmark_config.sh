#!/bin/bash
# Benchmark Configuration File
# Edit this file to control which benchmark combinations run
# Set to "true" to run, "false" to skip

# Queue mode with GrowableArray ON
RUN_QUEUE_GROWABLE_ON=true

# Queue mode with GrowableArray ON + WeakRef Agent
RUN_QUEUE_GROWABLE_ON_AGENT=false

# Queue mode with GrowableArray OFF
RUN_QUEUE_GROWABLE_OFF=true

# Queue mode with GrowableArray OFF + WeakRef Agent
RUN_QUEUE_GROWABLE_OFF_AGENT=false

# Poll mode with GrowableArray ON
RUN_POLL_GROWABLE_ON=false

# Poll mode with GrowableArray ON + WeakRef Agent
RUN_POLL_GROWABLE_ON_AGENT=false

# Poll mode with GrowableArray OFF
RUN_POLL_GROWABLE_OFF=false

# Poll mode with GrowableArray OFF + WeakRef Agent
RUN_POLL_GROWABLE_OFF_AGENT=false
