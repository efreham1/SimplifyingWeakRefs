\chapter{Results}
\label{chap:results}
In order to evaluate the effectiveness of the implemented optimisations, the two synthetic benchmarks described in \cref{subsec:benchmark-design} were executed as outlined in \cref{subsec:execution-environment} and the metrics listed in \cref{subsec:measurement-methodology} were collected.

The memory metrics, namely the average of the resident set size (RSS), GC committed memory, and Java heap size were measured and compared between the baseline and optimised implementations. The results of these measurements are presented in ...

The performance metrics, namely the average duration of major collections, old generation collections, old generation mark and follow, old generation reference processing, and young generation mark and follow were also measured and compared between the baseline and optimised implementations. The results of these measurements are presented in ...