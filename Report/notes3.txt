mention work done for getting real world benchmarks, and the challenges of doing so.
Future work, use JFR
Future work, check system load
Change millisecond-scale to sub-millisecond and cite https://inside.java/2023/08/31/generational-zgc-and-beyond/
G1 doesn’t do “sweeping”, it does the marking of the old generatoin concurrently and then all compaction (copying) during STW pauses. But the fact that it can do the liveness information concurrently and has remembered sets, allows it to collect the old generation over a set of garbage collections instead of one long (since copying is the expensive part)

I don’t think you need to write all this. But sweeping should go, and the pauses do more than syncronization, in ZGC the pauses are about sync in G1 the handle the evacuation.

I would say something like, G1 performs marking concurrently with the application allowing it to reduce STW pause times.

Specify reference processing in generational GC, last sentence.

With generational ZGC the metadata is actually in the low-order bits: https://openjdk.org/jeps/439

Switch concurrent-mark "regression" to "impact"

Mention trade-off of returning array memory or not
