\chapter{Evaluation}
\label{chap:evaluation}

The implemented optimisations described in \cref{chap:design-and-implementation} were evaluated through custom microbenchmarks designed to synthetically target the reference processing pipeline. The benchmarks were executed on a supercomputer cluster under controlled conditions, and the resulting GC timing statistics and memory usage metrics were collected to enable comparison between the baseline and optimised variants.

\section{Optimisation Variants}
\label{sec:optimisation-variants}

Nine optimisation variants were evaluated in total. Eight of them cover all combinations of the three \texttt{WeakReference} pipeline optimisations described in \cref{chap:design-and-implementation}: the optimised clear path, skipping enqueue for queue-less references via separate discovered lists, and the dynamic array. \Cref{tab:eval-variants} shows which optimisations each variant enables. The \texttt{none} variant serves as the unmodified baseline. The remaining variants enable a subset of the three optimisations, allowing their individual and combined effects to be isolated. The \texttt{all} variant enables all three simultaneously. The ninth and final variant, \texttt{weak\_fields}, corresponds to the \texttt{@weak} field annotation described in \cref{sec:weak-fields-method}.

\begin{table}[t]
\centering
\caption[Optimisation variants]{Optimisation variants and their enabled optimisations. A tick indicates the optimisation is active; a dash indicates it is disabled.}
\label{tab:eval-variants}
\begin{tabularx}{\textwidth}{lXXX}
\toprule
\textbf{Variant} & \textbf{Optimised Clear Path} & \textbf{Skip Enqueue} & \textbf{Dynamic Array} \\
\midrule
\texttt{none}             & \textemdash & \textemdash & \textemdash \\
\texttt{dyn\_only}        & \textemdash & \textemdash & \checkmark  \\
\texttt{sep\_only}        & \textemdash & \checkmark  & \textemdash \\
\texttt{sep\_dyn}         & \textemdash & \checkmark  & \checkmark  \\
\texttt{clear\_path\_only}& \checkmark  & \textemdash & \textemdash \\
\texttt{clear\_path\_dyn} & \checkmark  & \textemdash & \checkmark  \\
\texttt{clear\_path\_sep} & \checkmark  & \checkmark  & \textemdash \\
\texttt{all}              & \checkmark  & \checkmark  & \checkmark  \\
\texttt{weak\_fields}     & \textemdash & \textemdash & \textemdash \\
\bottomrule
\end{tabularx}
\end{table}

Each variant is implemented as a set of patches under \texttt{patches/<variant>/}. All variants except for \texttt{dyn\_only} and \texttt{weak\_fields} also include a shared base patch with infrastructure changes common to those optimisations. \Cref{tab:patch-sizes} lists the resulting diff sizes; full per-file breakdowns are available in \cref{app:patch-details}. A build script (\texttt{scripts/build\_configs.sh}) automates applying the appropriate patches and building each JDK binary.

\begin{table}[t]
\centering
\caption[Patch sizes per variant]{Files changed and diff size per variant relative to upstream. Variants marked with $\dagger$ include the shared base patch (8~files, +43/$-$16~lines) in their totals.}
\label{tab:patch-sizes}
\begin{tabular}{lrrrrrr}
\toprule
\textbf{Variant} & \textbf{Modified} & \textbf{New} & \textbf{Ins.} & \textbf{Del.} & \textbf{Net} \\
\midrule
\texttt{all}$^\dagger$             & 10 & 1 & +397 & $-$68  & +329 \\
\texttt{clear\_path\_dyn}$^\dagger$  & 10 & 1 & +399 & $-$84  & +315 \\
\texttt{clear\_path\_only}$^\dagger$ & 10 & 0 & +188 & $-$41  & +147 \\
\texttt{clear\_path\_sep}$^\dagger$  & 10 & 0 & +214 & $-$46  & +168 \\
\texttt{dyn\_only}        & 2  & 1 & +205 & $-$69  & +136 \\
\texttt{sep\_dyn}$^\dagger$         & 10 & 1 & +360 & $-$61  & +299 \\
\texttt{sep\_only}$^\dagger$        & 10 & 0 & +173 & $-$43  & +130 \\
\texttt{weak\_fields}     & 28 & 2 & +748 & $-$232 & +516 \\
\bottomrule
\end{tabular}
\end{table}

\section{Performance Evaluation}
\label{sec:performance-evaluation}

To evaluate the performance impact of the optimisation approaches, four microbenchmarks were used under controlled conditions: two \texttt{WeakReference}-based benchmarks and two weak-field counterparts with analogous workload structure. The benchmarks and their execution environment are described below.

\subsection{Benchmark Design}
\label{subsec:benchmark-design}

Two benchmark families were used to evaluate the optimisation approaches. The first family isolates reference-processing cost around a single shared referent, and the second is a more realistic multi-object workload that introduces greater allocation and liveness variability. Each family was implemented both with \texttt{WeakReference} objects and with weak fields. In order to enable \texttt{WeakReference}-to-weak-field comparison, both benchmark families use holder objects that either contain a \texttt{WeakReference} or an annotated weak field.

\subsubsection{Single-Object Benchmark}
\label{subsubsec:single-object-benchmark}

The Single-Object benchmarks allocate 10 million holder objects with either a \texttt{WeakReference} object or an annotated weak field each that all point to a single target object. The holder objects are allocated and stored in a randomised order using a Fisher-Yates shuffle~\cite{knuth97-taocp2} to avoid artificial locality patterns. None of the weak references are created with a \texttt{ReferenceQueue}. After a brief holding period, the single strong reference to the target object is released and a GC cycle is triggered manually. Because all weak references become clearable simultaneously in a single GC cycle, this benchmark maximises the per-cycle reference processing load and isolates the cost of the processing pipeline itself.

\subsubsection{Multi-Object Benchmark}
\label{subsubsec:multi-object-benchmark}

The multi-object benchmarks represent a slightly more realistic workload. They allocate approximately 7.3 million objects, each containing a randomly sized byte array payload (between 509 and 1097 bytes). Each object has a strong reference and a corresponding holder object with either a \texttt{WeakReference} object or an annotated weak field. The allocation and storing order of both strong references and holder objects are randomised using separate Fisher-Yates shuffles~\cite{knuth97-taocp2} to avoid artificial locality patterns.

Unlike the Single-Object benchmark, the strong references are not all released at once. Instead, the benchmark proceeds through five rounds of clearing. In each round, 20\% of the strong references are nulled in a randomised order, making the corresponding objects eligible for garbage collection. Between rounds, the benchmark sleeps to allow GC cycles to discover and process the weak references whose referent now is unreachable. This gradual release pattern means that weak references/fields are discovered and processed across multiple GC cycles rather than in a single burst.

\subsection{Execution Environment}
\label{subsec:execution-environment}

The benchmarks were executed on UPPMAX, a supercomputer cluster at Uppsala University. \Cref{tab:cluster-specs} lists the hardware specification of the cluster nodes. Each variant was run using its pre-built JDK binary (see \cref{sec:optimisation-variants}). \Cref{tab:jvm-options} lists the JVM options used for each benchmark family.

\begin{table}[t]
\centering
\caption{Cluster node hardware specification}
\label{tab:cluster-specs}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Property} & \textbf{Value} \\
\midrule
Total nodes & 80 \\
CPU & AMD EPYC 9454P (Zen 4), 2.75\,GHz \\
Physical cores & 48 \\
Threads & 96 \\
Memory & 768\,GiB \\
Scratch & 1.7\,TiB \\
GPUs & None \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{JVM options per benchmark family}
\label{tab:jvm-options}
\begin{tabularx}{\textwidth}{@{}llX@{}}
\toprule
\textbf{Option} & \textbf{Benchmark} & \textbf{Purpose} \\
\midrule
\texttt{-Xms7g -Xmx7g}        & Single & Fixed 7\,GB heap \\
\texttt{-Xms8g -Xmx8g}        & Multi  & Fixed 8\,GB heap \\
\texttt{-XX:+UseZGC}           & Both   & Enable ZGC \\
\texttt{-XX:InitialTenuringThreshold=1}  & Both & Promote objects to old generation after one cycle \\
\texttt{-XX:MaxTenuringThreshold=1}      & Both & Cap tenuring at 1 \\
\texttt{-Xlog:gc+stats,gc+ref} & Both   & GC statistics and reference processing logs \\
\texttt{-XX:NativeMemoryTracking=summary} & Both & Native Memory Tracking \\
\texttt{-XX:ZCollectionIntervalMajor=0.5} & Multi & Force major collection every 0.5\,s \\
\texttt{-XX:+ZCollectionIntervalOnly}     & Multi & Only timer-triggered major collections \\
\bottomrule
\end{tabularx}
\end{table}

A 10-second cooldown period was inserted between variant runs to allow the system to return to a stable state.

\subsection{Measurement Methodology}
\label{subsec:measurement-methodology}

Each benchmark was run for a total of 100 iterations. Jobs were submitted via SLURM and allocated two nodes running in exclusive mode. Hyperthreading was disabled at the SLURM level, so only the 48 physical cores per node were available. To parallelise execution across the two nodes, iterations were split across 8 parallel instances (4 per node). Each instance was pinned via \texttt{taskset} to a disjoint set of 10 JVM cores and 2 auxiliary cores (12 cores per instance), preventing interference between concurrently running instances. Each instance ran its share of the 100 iterations preceded by 1 warm-up iteration whose results were discarded. The warm-up executed the full benchmark workload for every variant. Within each measurement iteration, all nine optimisation variants were run in sequence with a 10-second cooldown between each. This interleaved ordering mitigates systematic drift in system conditions affecting one configuration more than others.

During each run, a memory monitoring script samples memory metrics from the process at high frequency. See \cref{tab:memory-monitoring-metrics} for the memory metrics collected.

In addition to the continuous memory samples, GC statistics are parsed from the log files of each run. For each GC metric, the parser extracts the Total Avg/Max pair and unit reported by ZGC. The single-object benchmark concentrates its entire weak-reference load into a single collection cycle, so the per-run \emph{maximum} is the relevant statistic. But since the multi-object benchmark spreads work across many cycles the per-run \emph{average} is used instead. \Cref{tab:gc-log-metrics} lists the GC log metrics collected.

\begin{table}[t]
\centering
\caption[Continuous monitoring metrics in the measurement pipeline]{Continuous monitoring metrics used in the measurement pipeline.}
\label{tab:memory-monitoring-metrics}
\begin{tabularx}{\textwidth}{@{}XlX@{}}
\toprule
\textbf{Metric} & \textbf{Units} & \textbf{Description} \\
\midrule
Timestamp & ms & Sample timestamp used to align and aggregate runs over time. \\
RSS & kB & Process resident set size (RSS), representing total physical memory in use by the JVM process. \\
GC Committed Memory & kB & GC-related memory currently committed by the JVM. \\
Java Heap & kB & Java heap usage. \\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}[t]
\centering
\caption[GC log metrics in the measurement pipeline]{GC log metrics used in the measurement pipeline. For the single-object benchmark, per-run maximum values are used; for the multi-object benchmark, per-run averages are used.}
\label{tab:gc-log-metrics}
\begin{tabularx}{\textwidth}{@{}XlX@{}}
\toprule
\textbf{Metric} & \textbf{Units} & \textbf{Description} \\
\midrule
Major Collection & ms & End-to-end duration of major collections. \\
Old Generation & ms & Total time spent in old-generation collection work. \\
Old Phase: Concurrent Mark & ms & Time spent in old-generation concurrent marking. \\
Old Phase: Concurrent Process Non-Strong & ms & Time spent processing non-strong references in the old generation. \\
Old Phase: Concurrent Relocate & ms & Time spent in old-generation concurrent relocation. \\
Young Generation\textsuperscript{\textdagger} & ms & Total time in young-generation collection. For multi-object, zero values (cycles without young-gen work) are excluded. \\
Young Phase: Concurrent Mark & ms & Time spent in young-generation concurrent marking. \\
Young Phase: Concurrent Relocate & ms & Time spent in young-generation concurrent relocation. \\
\bottomrule
\end{tabularx}
\smallskip
\noindent\textsuperscript{\textdagger}Multi-object uses ``Young Generation: Young Generation''; single-object uses ``Young Generation: Young Generation (Promote All)''.
\end{table}

\section{Presentation of Results}
\label{sec:results-presentation}

Results are presented for all nine variants listed in \cref{tab:eval-variants}: the unmodified baseline (\texttt{none}), the seven \texttt{WeakReference} pipeline variants, and \texttt{weak\_fields}. This enables two complementary comparisons:
\begin{itemize}
	\item \textbf{Intra-model comparison} among the \texttt{WeakReference} variants, isolating the effect of each optimisation and their interactions relative to the baseline.
	\item \textbf{Inter-model comparison} between the best-performing \texttt{WeakReference} variants and the weak-field representation, evaluating whether avoiding \texttt{WeakReference} objects altogether offers additional benefit.
\end{itemize}

Two families of plots are used. GC timing behaviour is shown as box plots (\cref{fig:gc-timing-single,fig:gc-timing-single-2,fig:gc-timing-single-3} for the single-object benchmark; \cref{fig:gc-timing-multi,fig:gc-timing-multi-2,fig:gc-timing-multi-3} for the multi-object benchmark). Each box spans the interquartile range (IQR) from the 25th to the 75th percentile, with a horizontal line at the median. Whiskers extend to the most extreme data point within $1.5 \times \mathrm{IQR}$ of the box edge, and points beyond the whiskers are plotted individually as outliers. For the single-object benchmark, each data point is the per-run maximum of the metric; for the multi-object benchmark it is the per-run average, following the rationale in \cref{subsec:measurement-methodology}.

Memory behaviour is shown as continuous time-series traces (\cref{fig:memory-single} for single-object; \cref{fig:memory-multi} for multi-object). Each trace plots the mean value across all runs as a solid line, with a shaded band showing $\pm 1$ standard deviation. Three metrics are tracked: total process RSS, GC auxiliary memory, and Java heap usage.

% ---------- GC timing box plots: single-object ----------
\begin{figure}[t]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_major_collection_major_collection_single_full-20260409-211258-single.pdf}
		\caption{Major Collection}
		\label{fig:bp-single-major}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_generation_old_generation_single_full-20260409-211258-single.pdf}
		\caption{Old Generation}
		\label{fig:bp-single-old-gen}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_mark_single_full-20260409-211258-single.pdf}
		\caption{Old Phase: Concurrent Mark}
		\label{fig:bp-single-old-mark}
	\end{subfigure}
	\caption[GC timing metrics (single-object, part 1)]{Box plots of GC timing metrics across evaluated configurations for the single-object benchmark (per-run maxima), part~1.}
	\label{fig:gc-timing-single}
\end{figure}

\begin{figure}[t]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_process_non-strong_single_full-20260409-211258-single.pdf}
		\caption{Old Phase: Concurrent Process Non-Strong}
		\label{fig:bp-single-old-nonstrong}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_relocate_single_full-20260409-211258-single.pdf}
		\caption{Old Phase: Concurrent Relocate}
		\label{fig:bp-single-old-relocate}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_generation_young_generation_promote_all_single_full-20260409-211258-single.pdf}
		\caption{Young Generation (Promote All)}
		\label{fig:bp-single-young-gen}
	\end{subfigure}
	\caption[GC timing metrics (single-object, part 2)]{Box plots of GC timing metrics across evaluated configurations for the single-object benchmark (per-run maxima), part~2.}
	\label{fig:gc-timing-single-2}
\end{figure}

\begin{figure}[t]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_phase_concurrent_mark_single_full-20260409-211258-single.pdf}
		\caption{Young Phase: Concurrent Mark}
		\label{fig:bp-single-young-mark}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_phase_concurrent_relocate_single_full-20260409-211258-single.pdf}
		\caption{Young Phase: Concurrent Relocate}
		\label{fig:bp-single-young-relocate}
	\end{subfigure}
	\caption[GC timing metrics (single-object, part 3)]{Box plots of GC timing metrics across evaluated configurations for the single-object benchmark (per-run maxima), part~3.}
	\label{fig:gc-timing-single-3}
\end{figure}

% ---------- GC timing box plots: multi-object ----------
\begin{figure}[t]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_major_collection_major_collection_multi_full-20260409-211258-multi.pdf}
		\caption{Major Collection}
		\label{fig:bp-multi-major}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_generation_old_generation_multi_full-20260409-211258-multi.pdf}
		\caption{Old Generation}
		\label{fig:bp-multi-old-gen}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_mark_multi_full-20260409-211258-multi.pdf}
		\caption{Old Phase: Concurrent Mark}
		\label{fig:bp-multi-old-mark}
	\end{subfigure}
	\caption[GC timing metrics (multi-object, part 1)]{Box plots of GC timing metrics across evaluated configurations for the multi-object benchmark (per-run averages), part~1.}
	\label{fig:gc-timing-multi}
\end{figure}

\begin{figure}[t]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_process_non-strong_multi_full-20260409-211258-multi.pdf}
		\caption{Old Phase: Concurrent Process Non-Strong}
		\label{fig:bp-multi-old-nonstrong}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_relocate_multi_full-20260409-211258-multi.pdf}
		\caption{Old Phase: Concurrent Relocate}
		\label{fig:bp-multi-old-relocate}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_generation_young_generation_multi_full-20260409-211258-multi.pdf}
		\caption{Young Generation}
		\label{fig:bp-multi-young-gen}
	\end{subfigure}
	\caption[GC timing metrics (multi-object, part 2)]{Box plots of GC timing metrics across evaluated configurations for the multi-object benchmark (per-run averages), part~2.}
	\label{fig:gc-timing-multi-2}
\end{figure}

\begin{figure}[t]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_phase_concurrent_mark_multi_full-20260409-211258-multi.pdf}
		\caption{Young Phase: Concurrent Mark}
		\label{fig:bp-multi-young-mark}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_phase_concurrent_relocate_multi_full-20260409-211258-multi.pdf}
		\caption{Young Phase: Concurrent Relocate}
		\label{fig:bp-multi-young-relocate}
	\end{subfigure}
	\caption[GC timing metrics (multi-object, part 3)]{Box plots of GC timing metrics across evaluated configurations for the multi-object benchmark (per-run averages), part~3.}
	\label{fig:gc-timing-multi-3}
\end{figure}

% ---------- Memory traces: single-object ----------
\begin{figure}[t]
	\centering
	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_rss_single_full-20260409-211258-single.pdf}
		\caption{Total Process RSS}
		\label{fig:mem-single-rss}
	\end{subfigure}

	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_gc_auxiliary_single_full-20260409-211258-single.pdf}
		\caption{GC Auxiliary Memory}
		\label{fig:mem-single-gc}
	\end{subfigure}

	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_java_heap_single_full-20260409-211258-single.pdf}
		\caption{Java Heap}
		\label{fig:mem-single-heap}
	\end{subfigure}
	\caption[Continuous memory traces (single-object)]{Continuous memory traces for the single-object benchmark (mean \textpm\ 1 std dev across runs).}
	\label{fig:memory-single}
\end{figure}

% ---------- Memory traces: multi-object ----------
\begin{figure}[t]
	\centering
	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_rss_multi_full-20260409-211258-multi.pdf}
		\caption{Total Process RSS}
		\label{fig:mem-multi-rss}
	\end{subfigure}

	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_gc_auxiliary_multi_full-20260409-211258-multi.pdf}
		\caption{GC Auxiliary Memory}
		\label{fig:mem-multi-gc}
	\end{subfigure}

	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_java_heap_multi_full-20260409-211258-multi.pdf}
		\caption{Java Heap}
		\label{fig:mem-multi-heap}
	\end{subfigure}
	\caption[Continuous memory traces (multi-object)]{Continuous memory traces for the multi-object benchmark (mean \textpm\ 1 std dev across runs).}
	\label{fig:memory-multi}
\end{figure}
