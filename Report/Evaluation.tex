\chapter{Evaluation}
\label{chap:evaluation}

The implemented optimisations described in \cref{chap:design-and-implementation} were evaluated through two custom microbenchmarks designed to synthetically target the reference processing pipeline. The benchmarks were executed on a supercomputer cluster under controlled conditions, and the resulting GC timing statistics and memory usage metrics were collected to enable comparison between the baseline and optimised variants.

\section{Build Configuration}
\label{sec:build-configurations}

Each optimisation variant is implemented as a separate set of modified source files stored under \texttt{patches/<variant>/src/}. Modifications shared by most variants reside in \texttt{patches/base/src/}. The \texttt{none} variant uses the unmodified upstream OpenJDK source and the \texttt{dyn\_only} variant only applies its own patches without the base patch. A build script (\texttt{scripts/build\_configs.sh}) automates building all variants: for each variant, it restores the upstream source, applies the appropriate patches, and invokes the JDK build.

Eight build configurations were created to evaluate all combinations of the three optimisations, as shown in \cref{tab:build-configs}. Each configuration produces a separate JDK build with its own binary, allowing direct comparison under identical benchmark conditions.

\begin{table}[t]
\centering
\caption[Build Variants]{Build variants and their enabled optimisations}
\label{tab:build-configs}
\begin{tabularx}{\textwidth}{l|XXX}
\toprule
\textbf{Variant} & \textbf{Using\ Optimised\ Clear\ Path} & \textbf{Skipping\ Enqueue} & \textbf{Using\ Dynamic\ Array} \\
\midrule
None      & \textemdash & \textemdash & \textemdash \\
Dyn & \textemdash  & \textemdash & \checkmark \\
Sep & \textemdash & \checkmark  & \textemdash \\
Sep + Dyn  & \textemdash & \checkmark  & \checkmark  \\
Clear Path & \checkmark  & \textemdash & \textemdash \\
Clear Path + Dyn  & \checkmark & \textemdash  & \checkmark  \\
Clear Path + Sep  & \checkmark  & \checkmark  & \textemdash \\
All       & \checkmark  & \checkmark  & \checkmark  \\
\bottomrule
\end{tabularx}
\end{table}

Weak fields were evaluated as a separate configuration in the same benchmark harness. In this setup, the \texttt{WeakReference} benchmark class is replaced by its weak-field counterpart using a dedicated \texttt{weak\_fields} build variant whose patches (\texttt{patches/weak\_fields/src/}) are applied independently of the base layer. This isolates representation-level effects from the three \texttt{WeakReference} pipeline optimisations, whilst still allowing direct comparison within the same execution and analysis workflow.

\section{Performance Evaluation}
\label{sec:performance-evaluation}

To evaluate the performance impact of the optimisation approaches, four microbenchmarks were used under controlled conditions: two \texttt{WeakReference}-based benchmarks and two weak-field counterparts with analogous workload structure. The benchmarks and their execution environment are described below.

\subsection{Benchmark Design}
\label{subsec:benchmark-design}

Two benchmark families were used to evaluate the optimisation approaches. The first family isolates reference-processing cost around a single shared referent, and the second is a more realistic multi-object workload that introduces greater allocation and liveness variability. Each family was implemented both with \texttt{WeakReference} objects and with weak fields. In order to enable \texttt{WeakReference}-to-weak-field comparison, both benchmark families use holder objects that either contain a \texttt{WeakReference} or an annotated weak field.

\subsubsection{Single-Object Benchmark}
\label{subsubsec:single-object-benchmark}

The Single-Object benchmarks allocate 10 million holder objects with either a \texttt{WeakReference} object or an annotated weak field each that all point to a single target object. The holder objects are allocated and stored in a randomised order using a Fisher-Yates shuffle~\cite{knuth97-taocp2} to avoid artificial locality patterns. None of the weak references are created with a \texttt{ReferenceQueue}. After a brief holding period, the single strong reference to the target object is released and a GC cycle is triggered manually. Because all weak references become clearable simultaneously in a single GC cycle, this benchmark maximises the per-cycle reference processing load and isolates the cost of the processing pipeline itself.

\subsubsection{Multi-Object Benchmark}
\label{subsubsec:multi-object-benchmark}

The multi-object benchmarks are a slightly more realistic workload. It allocates approximately 7.3 million objects, each containing a randomly sized byte array payload (between 509 and 1097 bytes). Each object has a strong reference and a corresponding holder object with either a \texttt{WeakReference} object or an annotated weak field. 5\% of the \texttt{WeakReference}s are created with a \texttt{ReferenceQueue} and 95\% without, reflecting a workload dominated by queue-less weak references but where the enqueue path is still exercised for a minority of references. The allocation and storing order of both strong references and holder objects are randomised using separate Fisher-Yates shuffles~\cite{knuth97-taocp2} to avoid artificial locality patterns.

Unlike the Single-Object benchmark, the strong references are not all released at once. Instead, the benchmark proceeds through five rounds of clearing. In each round, 20\% of the strong references are nulled in a randomised order, making the corresponding objects eligible for garbage collection. Between rounds, the benchmark sleeps to allow GC cycles to discover and process the weak references whose referent now is unreachable. This gradual release pattern means that weak references are discovered and processed across multiple GC cycles rather than in a single burst.

The benchmark runs three iterations with a five-second cooldown between them to allow the JVM to stabilise. Each iteration performs the full allocation, clearing, and GC cycle sequence independently.

\subsection{Execution Environment}
\label{subsec:execution-environment}

The benchmarks were executed on \texttt{TODO: cluster name}, a supercomputer cluster at \texttt{TODO: institution}. Each benchmark instance was allocated approximately 4~CPU cores and 16\,GB of memory. The cluster nodes run \texttt{TODO: OS and kernel version}. Each variant was run using its pre-built JDK binary (see \cref{sec:build-configurations}).

\Cref{tab:jvm-options} lists the JVM options used for each benchmark family. Both benchmarks share a common set of options; the multi-object benchmark adds a forced major-collection interval to ensure frequent GC activity. Tenuring thresholds are set to~1 so that objects are promoted to the old generation quickly, which is necessary because ZGC does not perform reference processing in the young generation~\cite{openjdk-zreferenceprocessor-cpp}.

\begin{table}[htbp]
\centering
\caption{JVM options per benchmark family}
\label{tab:jvm-options}
\begin{tabularx}{\textwidth}{@{}llX@{}}
\toprule
\textbf{Option} & \textbf{Benchmark} & \textbf{Purpose} \\
\midrule
\texttt{-Xms7g -Xmx7g}        & Single & Fixed 7\,GB heap \\
\texttt{-Xms8g -Xmx8g}        & Multi  & Fixed 8\,GB heap \\
\texttt{-XX:+UseZGC}           & Both   & Enable ZGC \\
\texttt{-XX:InitialTenuringThreshold=1}  & Both & Promote objects to old generation after one cycle \\
\texttt{-XX:MaxTenuringThreshold=1}      & Both & Cap tenuring at 1 \\
\texttt{-Xlog:gc+stats,gc+ref} & Both   & GC statistics and reference processing logs \\
\texttt{-XX:NativeMemoryTracking=summary} & Both & Native Memory Tracking \\
\texttt{-XX:ZCollectionIntervalMajor=0.5} & Multi & Force major collection every 0.5\,s \\
\texttt{-XX:+ZCollectionIntervalOnly}     & Multi & Only timer-triggered major collections \\
\bottomrule
\end{tabularx}
\end{table}

A 10-second cooldown period was inserted between variant runs to allow the system to return to a stable state. Between runs, filesystem caches were dropped by writing to \texttt{/proc/sys/vm/drop\_caches}.

\subsection{Measurement Methodology}
\label{subsec:measurement-methodology}

Each benchmark was run for a total of 100 outer iterations. To parallelise execution across the cluster, the iterations were split across multiple instances, where each instance ran $n$ measurement iterations preceded by 1 warm-up iteration whose results were discarded. Each warm-up iteration executed the full benchmark workload for every variant and served to prime JIT compilation and OS-level caches before measured runs began. Within each measurement iteration, all eight \texttt{WeakReference} variants were run in sequence with cooldown and cache clearing between each. In the same iteration, the weak-field benchmark variant was then executed as an additional run. This interleaved ordering mitigates systematic drift in system conditions affecting one configuration more than others.

During each run, a memory monitoring script samples memory metrics from the process at high frequency. Additionally, the current benchmark phase is tracked and for the multi-object benchmark the inner iteration is tracked as well. This tracking is done from the benchmark's log file to enable phase-aligned analysis. See \cref{tab:memory-monitoring-metrics} for the memory metrics collected.

In addition to the continuous memory samples, GC statistics are parsed from the log files of each run. For each GC metric, the parser extracts the Total Avg/Max pair and unit reported by ZGC. For the single-object benchmark, Max is used as the primary value for comparison; for the multi-object benchmark, Avg is used. The analysis pipeline also recognises the weak-field run naming convention and includes weak-field results as a dedicated \texttt{weak\_fields} configuration in comparative plots and summary tables. See \cref{tab:gc-log-metrics} for the GC log metrics collected.

\begin{table}[t]
\centering
\caption[Continuous monitoring metrics in the measurement pipeline]{Continuous monitoring metrics used in the measurement pipeline.}
\label{tab:memory-monitoring-metrics}
\begin{tabularx}{\textwidth}{@{}XlX@{}}
\toprule
\textbf{Metric} & \textbf{Units} & \textbf{Description} \\
\midrule
Timestamp & ms & Sample timestamp used to align and aggregate runs over time. \\
RSS & kB & Process resident set size (RSS), representing total physical memory in use by the JVM process. \\
GC Committed Memory & kB & GC-related memory currently committed by the JVM (actually backed memory from Native Memory Tracking). \\
Java Heap & kB & Java heap usage sampled from \texttt{jcmd GC.heap\_info}. \\
Phase & label & Benchmark phase label emitted by the benchmark and used for phase-aligned analysis. \\
Iteration & label & Inner-iteration identifier (multi-object benchmark) used to align repeated phase sequences. \\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}[t]
\centering
\caption[GC log metrics in the measurement pipeline]{GC log metrics used in the measurement pipeline. For the single-object benchmark, per-run maximum values are used; for the multi-object benchmark, per-run averages are used. The Young Generation metric differs between benchmarks as noted.}
\label{tab:gc-log-metrics}
\begin{tabularx}{\textwidth}{@{}XlX@{}}
\toprule
\textbf{Metric} & \textbf{Units} & \textbf{Description} \\
\midrule
Major Collection & ms & End-to-end duration of major collections. \\
Old Generation & ms & Total time spent in old-generation collection work. \\
Old Phase: Concurrent Mark & ms & Time spent in old-generation concurrent marking. \\
Old Phase: Concurrent Process Non-Strong & ms & Time spent processing non-strong references in the old generation. \\
Old Phase: Concurrent Relocate & ms & Time spent in old-generation concurrent relocation. \\
Young Generation\textsuperscript{\textdagger} & ms & Total time in young-generation collection. For multi-object, zero values (cycles without young-gen work) are excluded. \\
Young Phase: Concurrent Mark & ms & Time spent in young-generation concurrent marking. \\
Young Phase: Concurrent Relocate & ms & Time spent in young-generation concurrent relocation. \\
\bottomrule
\end{tabularx}
\smallskip
\noindent\textsuperscript{\textdagger}Multi-object uses ``Young Generation: Young Generation''; single-object uses ``Young Generation: Young Generation (Promote All)''.
\end{table}

\section{Presentation of Results}
\label{sec:results-presentation}

This section presents the empirical results of the performance evaluation. The comparison covers the baseline (\texttt{none}) and all seven non-empty combinations of the three optimisations (clear path, separate lists, dynamic array) for \texttt{WeakReference}-based processing. In addition, a dedicated \texttt{weak\_fields} configuration is included, where the weak-field benchmark classes are executed in the same harness and analysed with the same tooling. This configuration set enables two complementary comparisons:
\begin{itemize}
	\item \textbf{Intra-model comparison} among \texttt{WeakReference} variants to isolate the impact of each optimisation and their interactions.
	\item \textbf{Inter-model comparison} between optimised \texttt{WeakReference} processing and weak-field representation to evaluate structural overhead trade-offs.
\end{itemize}

The primary performance metrics are the GC timing statistics extracted from ZGC logs, including major and old-generation collection time, concurrent mark, non-strong processing, and relocation phases for both old and young generations. Memory behaviour is characterised using continuous samples of RSS, GC committed memory, and Java heap usage. Following the methodology in \cref{subsec:measurement-methodology}, single-object benchmark comparisons use the per-run maximum values as primary statistics, while multi-object comparisons use per-run averages. This choice reflects the fact that single-object runs are designed to create concentrated peak load in one collection event, whereas multi-object runs distribute work over repeated phases.

\Cref{fig:gc-timing-single,fig:gc-timing-multi} show box plots of the GC timing metrics for the single-object and multi-object benchmarks. They summarise the distribution of per-run maxima for the single-object benchmark and per-run averages for the multi-object benchmark, allowing comparison of both central tendency and variability. \Cref{fig:memory-single,fig:memory-multi} show continuous-memory traces for the same benchmark families, illustrating how memory usage evolves over time.

% ---------- GC timing box plots: single-object ----------
\begin{figure}[p]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_major_collection_major_collection_single_1.pdf}
		\caption{Major Collection}
		\label{fig:bp-single-major}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_generation_old_generation_single_1.pdf}
		\caption{Old Generation}
		\label{fig:bp-single-old-gen}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_mark_single_1.pdf}
		\caption{Old Phase: Concurrent Mark}
		\label{fig:bp-single-old-mark}
	\end{subfigure}
	\caption[GC timing metrics (single-object, part 1)]{Box plots of GC timing metrics across evaluated configurations for the single-object benchmark (per-run maxima), part~1.}
	\label{fig:gc-timing-single}
\end{figure}

\begin{figure}[p]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_process_non-strong_single_1.pdf}
		\caption{Old Phase: Concurrent Process Non-Strong}
		\label{fig:bp-single-old-nonstrong}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_relocate_single_1.pdf}
		\caption{Old Phase: Concurrent Relocate}
		\label{fig:bp-single-old-relocate}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_generation_young_generation_promote_all_single_1.pdf}
		\caption{Young Generation (Promote All)}
		\label{fig:bp-single-young-gen}
	\end{subfigure}
	\caption[GC timing metrics (single-object, part 2)]{Box plots of GC timing metrics across evaluated configurations for the single-object benchmark (per-run maxima), part~2.}
	\label{fig:gc-timing-single-2}
\end{figure}

\begin{figure}[p]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_phase_concurrent_mark_single_1.pdf}
		\caption{Young Phase: Concurrent Mark}
		\label{fig:bp-single-young-mark}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_phase_concurrent_relocate_single_1.pdf}
		\caption{Young Phase: Concurrent Relocate}
		\label{fig:bp-single-young-relocate}
	\end{subfigure}
	\caption[GC timing metrics (single-object, part 3)]{Box plots of GC timing metrics across evaluated configurations for the single-object benchmark (per-run maxima), part~3.}
	\label{fig:gc-timing-single-3}
\end{figure}

% ---------- GC timing box plots: multi-object ----------
\begin{figure}[p]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_major_collection_major_collection_multi_2.pdf}
		\caption{Major Collection}
		\label{fig:bp-multi-major}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_generation_old_generation_multi_2.pdf}
		\caption{Old Generation}
		\label{fig:bp-multi-old-gen}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_mark_multi_2.pdf}
		\caption{Old Phase: Concurrent Mark}
		\label{fig:bp-multi-old-mark}
	\end{subfigure}
	\caption[GC timing metrics (multi-object, part 1)]{Box plots of GC timing metrics across evaluated configurations for the multi-object benchmark (per-run averages), part~1.}
	\label{fig:gc-timing-multi}
\end{figure}

\begin{figure}[p]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_process_non-strong_multi_2.pdf}
		\caption{Old Phase: Concurrent Process Non-Strong}
		\label{fig:bp-multi-old-nonstrong}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_old_phase_concurrent_relocate_multi_2.pdf}
		\caption{Old Phase: Concurrent Relocate}
		\label{fig:bp-multi-old-relocate}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_generation_young_generation_multi_2.pdf}
		\caption{Young Generation}
		\label{fig:bp-multi-young-gen}
	\end{subfigure}
	\caption[GC timing metrics (multi-object, part 2)]{Box plots of GC timing metrics across evaluated configurations for the multi-object benchmark (per-run averages), part~2.}
	\label{fig:gc-timing-multi-2}
\end{figure}

\begin{figure}[p]
	\centering
	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_phase_concurrent_mark_multi_2.pdf}
		\caption{Young Phase: Concurrent Mark}
		\label{fig:bp-multi-young-mark}
	\end{subfigure}

	\begin{subfigure}[b]{0.7\textwidth}
		\includegraphics[width=\textwidth]{boxplot_young_phase_concurrent_relocate_multi_2.pdf}
		\caption{Young Phase: Concurrent Relocate}
		\label{fig:bp-multi-young-relocate}
	\end{subfigure}
	\caption[GC timing metrics (multi-object, part 3)]{Box plots of GC timing metrics across evaluated configurations for the multi-object benchmark (per-run averages), part~3.}
	\label{fig:gc-timing-multi-3}
\end{figure}

% ---------- Memory traces: single-object ----------
\begin{figure}[p]
	\centering
	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_rss_single_1.pdf}
		\caption{Total Process RSS}
		\label{fig:mem-single-rss}
	\end{subfigure}

	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_gc_auxiliary_single_1.pdf}
		\caption{GC Auxiliary Memory}
		\label{fig:mem-single-gc}
	\end{subfigure}

	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_java_heap_single_1.pdf}
		\caption{Java Heap}
		\label{fig:mem-single-heap}
	\end{subfigure}
	\caption[Continuous memory traces (single-object)]{Continuous memory traces for the single-object benchmark (mean \textpm\ 1 std dev across runs).}
	\label{fig:memory-single}
\end{figure}

% ---------- Memory traces: multi-object ----------
\begin{figure}[p]
	\centering
	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_rss_multi_2.pdf}
		\caption{Total Process RSS}
		\label{fig:mem-multi-rss}
	\end{subfigure}

	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_gc_auxiliary_multi_2.pdf}
		\caption{GC Auxiliary Memory}
		\label{fig:mem-multi-gc}
	\end{subfigure}

	\begin{subfigure}[b]{0.8\textwidth}
		\includegraphics[width=\textwidth]{memory_java_heap_multi_2.pdf}
		\caption{Java Heap}
		\label{fig:mem-multi-heap}
	\end{subfigure}
	\caption[Continuous memory traces (multi-object)]{Continuous memory traces for the multi-object benchmark (mean \textpm\ 1 std dev across runs).}
	\label{fig:memory-multi}
\end{figure}
